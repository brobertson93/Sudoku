import { NegativeConvertPipe } from './negative-convert.pipe';

describe('NegativeConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new NegativeConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
